#include "main.h"
#include <ctype.h>

/**
* _isupper - this function checks for uppercase ASCII characters
* @c: This is an ASCII character
* This code dey sweet me die
* Return: 0 if uppercase, else 1
*/
int _isupper(int c)
{
if (isupper(c))
return (1);
return (0);
}
